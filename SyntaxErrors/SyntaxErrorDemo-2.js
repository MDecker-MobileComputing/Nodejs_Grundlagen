/*
 * This program is not executed because it contains a syntax error in the else block.
 * Because of the syntax error in the last line, the whole file is not executed.
 */


const number = 123;

if ( number > 100 ) {

    console.log( "Number is greater than 100." );

} else {

    console.log( "Number is less than or equal to 100."
}